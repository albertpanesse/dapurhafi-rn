import Types from './types';

const initialState = {
  ISOcode: "ID",
  callingCode: "62",
};

const reducer = (state = initialState, action) => {
	switch(action.type) {
		default:
			return state;
	}
};

export default reducer;
