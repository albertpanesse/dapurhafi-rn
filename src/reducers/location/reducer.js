import Types from './types';

const initialState = {
	userLocation: null
};

const reducer = (state = initialState, action) => {
	switch(action.type) {
		default:
			return state;
	}
};

export default reducer;
